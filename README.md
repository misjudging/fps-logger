# FPS Logger

Quick Python tool to calculate FPS stats from frame times (ms).

## Input format

Text file with one frame time per line in milliseconds.

## Run

```bash
python fps_logger.py --input frametimes.txt
```
